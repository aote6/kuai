# Kuai Project Status

## Phase 3.0 — 变量系统 + 条件跳过

109 tests, 0 failures.

### 新增能力

| 块 | 类型 | 说明 |
|---|---|---|
| 设置变量 | 辅助 | 将 State 中某个字段的值另存为新 key |
| 取变量 | 辅助 | 从 State 读取指定 key，存入目标 key |
| 条件跳过 | 控制 | 检查字段值，匹配时跳过后续所有块 |

### 核心机制

- **SkipRemainingSteps**：Engine 捕获此异常后跳过剩余块，不报错
- **变量 = State key**：无第二套存储，无作用域，无变量表
- **参数自动清理**：`_` 前缀字段执行后移除

### 测试体系 (109 tests)

| 层级 | 数量 |
|---|---|
| Block 单元测试 | 85 |
| Flow 集成测试 | 11 |
| 系统不变量测试 | 11 |
| 综合场景测试 | 2 |

### 块清单 (24 个)

进入 列表 筛选 选取 复制 移动 重命名 批量重命名 压缩 解压 删除
合并为对象 读取 替换 清理 同步 发送 分类 按日期分类 生成报告
设置变量 取变量 条件跳过

（调试块：冲突A/冲突B/参数测试，建议删除）

### 设计原则

1. State 是唯一数据载体
2. 最小修改原则：块只碰它必须碰的字段
3. 字段所有权：每个 State key 有唯一所有者
4. 语法零膨胀：分支/变量都是普通块，不是新语法

### 已知限制

- 无循环结构（批量块内部循环够用）
- 参数注入为全局预处理，非按需延迟注入
- 无任务执行记录
- 无错误恢复/回滚

### 下一步

- 等真实需求驱动，不加功能
- 可考虑：AI决策块、循环、复合块封装

*最后更新：Phase 3.0 — 变量系统 + 条件跳过完成*
